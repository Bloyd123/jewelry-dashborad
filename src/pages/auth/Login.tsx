// import { LoginFormValues, LoginResponse } from '@/types/auth.types';

// const handleLogin = async (data: LoginFormValues) => {
//   const response = await authService.login(data);
//   // response is type-safe: LoginResponse
// };